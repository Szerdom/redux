import React from 'react';
import { NavLink } from 'react-router-dom';

const NotFoundPage = () => (
    <div>
        not found component - <NavLink to="/">Go home</NavLink>
    </div>
);

export default NotFoundPage;
